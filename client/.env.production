# En production, l'Api est servie par la même origine que le client (même conteneur) : URL relative.
VITE_API_BASE_URL=

# VITE_CARTO_API_KEY n'est pas défini ici (fichier suivi par git, repo public) : il est injecté
# au build via --build-arg (voir Dockerfile) pour ne jamais committer la clé en clair.
